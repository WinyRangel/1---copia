import { ProveedorfilterPipe } from './proveedorfilter.pipe';

describe('ProveedorfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProveedorfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
